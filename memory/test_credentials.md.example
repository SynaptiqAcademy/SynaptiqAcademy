# SYNAPTIQ Test Credentials — DO NOT COMMIT TO GITHUB.
# Copy structure to test_credentials.md (gitignored) for local QA only.

## Seeded Admin
- email: admin@example.com
- password: <set at seed time via ADMIN_PASSWORD env>
- role: admin
- rotate before first production deploy

## Demo researcher (auto-seeded for QA)
- email: demo.user@example.com
- password: demo123
- role: user
- intent: smoke-test the platform end-to-end
- safe to disable in production

## Notes
- Backend auth uses httpOnly cookies (access_token + refresh_token).
- Frontend axios calls use `withCredentials: true`.
